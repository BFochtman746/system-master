# LRN-CUR-PRODUCTION-BINDING-002 / T1 — Exact Handler Materialization + Atomic Mutation Rebind

**Standing:** CANDIDATE BUILD PASS ON CHANGED SURFACE / NOT INSTALLED / NOT PRODUCTION COMPLETE

## Authority reconciliation

The old `LEARNING-FULL-STANDARD-001E` Batch-07 delegation is not used as the build selector for this tranche. The recovered, user-authorized Learning lineage has already advanced through the ownership rebase/refreeze, Learning/Curriculum physical separation, owner-separated remediation seam and Production Binding 001.

The historical ownership denominator of 110 P0 requirements / 110 interfaces / 26 semantic objects was losslessly split to the active denominator of **113 requirements / 112 interfaces / 28 semantic objects**. Learning owns learner state/mastery/adaptation; Curriculum owns program/instruction/curriculum semantics; Experience owns human presentation; shared authorities retain evidence, jobs, reconciliation, authorization, rights/artifacts, lifecycle, security, model/provenance and cross-domain alignment truth.

The current dependency-valid build objective is therefore `LRN-CUR-PRODUCTION-BINDING-002`, not a replay of 001B or Batch 07.

## T1 build

T1 materializes three exact frozen inbound interfaces whose present owner-separated runtime semantics can be bound without inventing shared or cross-domain authority:

- `I001 CreateLearningGoal` — Learning-owned; strict payload; horizon/priority preserved; uses the owner-scoped atomic mutation bridge when DATA-001 production persistence supports it.
- `I016-L CreateRemediationNeed` — Learning-owned; evidence/skill references only; creates no instructional content and no mastery claim; atomic object + operation receipt + outbox event candidate path.
- `I027 GetCurriculum` — Curriculum-owned immutable read through the Curriculum query port; no Learning store access.

All other domain routes remain fail-closed in Master Core unless an exact handler is separately bound. Shared-owner routes remain delegated to canonical shared gateways.

## Atomicity repair

`OwnerScopedRepository` now exposes an owner-checked bridge to the already-defined DATA-001 `atomic_mutation` building block. It refuses foreign object/event kinds and refuses to treat historical Lab repositories as production atomic authority. Existing portable behavior falls back to the prior non-production path when no atomic backend exists, preserving regression behavior without relabeling it production-atomic.

## Qualification

Fresh changed-surface results:

- PB002-T1 tests: **9/9 PASS**
- REBIND-001: **20/20 PASS**
- REBIND-002: **34/34 PASS**
- IMPL016 owner-separated seam: **25/25 PASS**
- Production Binding suite after T1: **24/24 PASS** (15 prior + 9 T1)
- Changed-surface cumulative unique denominator: **103/103 PASS**

A fresh full historical 481-suite execution was attempted but did not complete within the available execution window, so **no fresh 481/481 claim is made for this tranche**. The prior forensic audit's 481/481 remains historical exact-source evidence only.

## Coverage truth

The frozen route constitution contains 63 inbound command/query routes:

- 59 Learning/Curriculum domain routes
- 4 shared-authority routes
- 3 candidate exact domain handlers bound in T1
- 56 domain handlers remain
- 4 shared routes still require real canonical gateways

T1 therefore advances handler materialization from **0/63 to candidate 3/63**, but does not claim production installation, live PostgreSQL, Master Core transport deployment, native iPhone behavior, human/real-learner evidence, psychometric validity, SME approval, certification, external-provider success or A-01 qualification.

## Important design blocker discovered

`I016-C RequestRemediationPlan` has a real portable owner-separated implementation, but the frozen production interface accepts a `remediation_need_ref`, while the current Curriculum runtime consumes the full immutable Learning-owned need value. Production Binding 002 must not let Curriculum read Learning persistence directly. T2 must therefore bind an exact cross-domain reference-resolution/value-transfer contract through Master Core or another admitted shared mechanism before `I016-C` can be counted as an exact production handler.

## Exact next operation

`LRN-CUR-PRODUCTION-BINDING-002-T2 — EXACT I016-C CROSS-DOMAIN REMEDIATION-NEED REF RESOLUTION + ADDITIONAL OWNER-VALID HANDLER MATERIALIZATION / ATOMIC UOW EXPANSION`
