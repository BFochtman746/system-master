# Invalid Run Reasons

A run is invalid, rather than merely low-scoring, if its measurement integrity is broken. Examples include hidden evaluator exposure before freeze, human assistance during the coding window, unenforced deadline, post-deadline mutation in the measured artifact, missing exact-subject identity, evaluator bytes not matching the sealed digest, or loss/overwrite of required raw evidence.

An invalid run must not be converted into a capability conclusion.
