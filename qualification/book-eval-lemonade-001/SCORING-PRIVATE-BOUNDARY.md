# Scoring-private boundary

The scoring-private package is deliberately NOT present in this repository branch or in the A-01 preflight workspace.

Previously sealed package identity from the Book evaluator qualification work:

- package: `BOOK_EVAL_SCORING_PRIVATE_v2.zip`
- SHA-256: `1091d409b5167463325ff85764bdd9b62c0e96cf9a9bef5f03445616c6b81940`

This document records identity only. It does not assert that the private package has been independently re-read from GitHub because it is intentionally absent here.

The private scoring material may be opened only after a blind run reaches its frozen-output closure state.
