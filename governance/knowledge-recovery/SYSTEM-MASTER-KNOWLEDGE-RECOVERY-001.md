# SYSTEM-MASTER-KNOWLEDGE-RECOVERY-001

**Effective date:** 2026-09-11  
**Current authority reconciliation:** 2026-09-14  
**Standing:** ACTIVE — HIGHEST DISCRETIONARY SYSTEM MASTER PRIORITY — PROGRAMMING PROVING CORPUS FIRST  
**Semantic owner:** `SYSTEM_MASTER` product-root governance  
**Execution administrator:** `SYSTEM_MASTER/CORE`  
**Verification executor:** shared `A-01` control plane  
**First proving corpus:** `PROGRAMMING`

## Priority lock

While this control is selected by `governance/CURRENT-AUTHORITY.json`, `SYSTEM-MASTER-KNOWLEDGE-RECOVERY-001` is the highest discretionary System Master priority.

Non-discretionary safety, current-authority reconciliation, exact owner integrity, active repair/control obligations, and human/private/native/external boundaries remain non-preemptible. Independent peer work continues in each peer's own lane.

Within discretionary System Master/CORE capacity, execute in this order:

1. recover and prove missing Programming source/custody/provenance evidence;
2. finish the Programming external-byte, identity, custody, manifest, asset-catalog and trace/provenance path to the next durable boundary;
3. then recover the remaining authorized catalog/archive sources using the same source-custody discipline;
4. only then select lower-priority discretionary Core discovery/research work.

Knowledge Recovery is support infrastructure. It does not own Programming product semantics, does not replace the Programming peer lane, and cannot transfer historical PASS to changed bytes or changed authority subjects.

## Current architecture boundary

Current authority selects `SYSTEM-TOPOLOGY-007` with exactly nine active peer systems:

- `CORE`
- `LEARNING`
- `BOOK`
- `DOCUMENTS`
- `SPREADSHEET_DATA`
- `MEDIA`
- `CONNECTED_ACTIONS`
- `RESEARCH_KNOWLEDGE`
- `PROGRAMMING`

`PROGRAMMING` is an admitted, incomplete peer system. Its preserved work-program evidence is continuity input and must not be restarted merely because peer admission occurred.

`WEBSITE_BUILDING` remains capability `C40` under `SYSTEM_MASTER/PROGRAMMING`. It is not a tenth peer. Browser/action side-effect authority remains with `CONNECTED_ACTIONS`, and external side effects still require explicit user authority.

`PROSE` is historically complete and terminally retired. It has no active owner path, child, execution lane, repair lane, qualification lane, research task, telemetry lane, successor task, or mutation claim domain. Any genuinely unfinished integration of preserved completed Prose capability is ordinary `SYSTEM_MASTER/BOOK` work. `DOCUMENTS` receives no Prose work.

Historical metadata that describes Programming as non-peer or Prose as active is preserved as provenance when it is evidence-bearing, but it is stale for current dispatch and cannot override `CURRENT-AUTHORITY-005` / `SYSTEM-TOPOLOGY-007`.

## Objective

Build and operate deterministic source-ingest, manifest, asset-catalog and trace/provenance recovery so System Master reuses what already exists before new research or implementation. Programming remains the proving corpus, while current Programming engineering work executes under the Programming peer owner.

Every source must preserve original locator, source identity/version, observed bytes when available, byte length, digest, conflicts, duplicate/supersession relations, custody state and explicit non-claims.

## Preserved supervised proving evidence

The previously accepted supervised proving subject remains exact historical evidence and is not relabeled:

- topology at execution: `SYSTEM-TOPOLOGY-004`;
- exact subject SHA: `4a2d4f7168f48728ef67d187ff43afb97c9568d9`;
- exact control-plane SHA: `c23dec17b907a3bdf6c1fac8ca1a6f829abea19f`;
- manifest: `PROGRAMMING-INGEST-MANIFEST-002`;
- workflow run: `34548900531`;
- executor: A-01 / Windows / X64;
- admission/result: ADMITTED / PASS, including same-subject deterministic repeat;
- durable acceptance record: `governance/knowledge-recovery/SYSTEM-MASTER-KNOWLEDGE-RECOVERY-001-SUPERVISED-ACCEPTANCE.json`.

Canonical output digests from that exact subject remain preserved:

- asset catalog: `6e7b66655678f09ba3835ec50fea68a48e267741e3dc3503c6bcea5df166422f`;
- trace graph: `dadbb337037e399c3aaf66b81dbe5171c2a6011c2b819f6f40614feed86809a9`;
- source observations: `ac3435de9e1028b1aeee55d7546463eaa94e84671d78fa201eb4ebbfaddc4efe`.

That acceptance proves the deterministic pipeline only for its exact Topology-004 subject. Topology changes do not transfer PASS to a changed subject. Fresh qualification is required for any new frozen current-authority subject.

## Programming corpus lineage and current gap

`PROGRAMMING-INGEST-MANIFEST-001`, `PROGRAMMING-INGEST-MANIFEST-002`, and `PROGRAMMING-INGEST-MANIFEST-003` are append-only lineage records.

Manifest 003 is a historical predecessor prepared under the then-current Topology-005 boundary. It preserves Manifests 001 and 002 and explicitly records that historical PASS does not transfer to Manifest 003. It must not be edited or relabeled to pretend it is a Topology-007 qualification subject.

The unresolved custody census carried forward from Manifest 003 remains useful recovery input:

- 26 inherited external-byte-pending records;
- 5 exact external objects were observed and hashed in the supervised workspace checkpoint, but 0 were staged in an approved immutable A-01-readable intake location;
- 21 external objects remained not yet observed;
- 2 historical records remained `IDENTITY_UNPROVEN`;
- 0 exact external objects were A-01 verified;
- full historical byte verification remained false.

The five observed objects remain progress, not terminal custody. Their active blocker is `REPOSITORY_BINARY_TRANSPORT_BOUNDARY`. A Library file id/version, filename, chat reference, workspace hash or pre-stage digest remains discovery/observation evidence until exact bytes cross an approved custody path. `A01_VERIFIED` requires A-01 to actually read those exact bytes.

A fresh Topology-007 qualification subject, if and when needed, must be created as an append-only successor (for example Manifest 004 or later) after current-authority inputs and custody dispositions are frozen. Historical receipts and manifests are never rewritten to achieve that transition.

## Second Shift decision

Knowledge Recovery is Second-Shift eligible under the CORE lane for deterministic, owner-valid, unattended-safe custody/provenance work. Programming product work uses the PROGRAMMING peer lane. Knowledge Recovery support does not create a second Programming claim domain and does not take Programming canonical state.

Second Shift may inventory unresolved Programming sources; retrieve/materialize through already-authorized source access; hash bytes actually observed; stage exact custody where supported; build versioned manifests/catalog/trace candidates; detect duplicates/conflicts; preserve source-specific blockers; and prepare exact-subject A-01 qualification requests.

Second Shift may not fabricate bytes or identities; claim A-01 verification before A-01 reads the bytes; change topology; relabel a historical receipt; resurrect Prose; route Prose work to Documents; bypass human/private/native/external authority; or infer publication/production authority.

## Required invariants

1. Architecture comes only from `CURRENT-AUTHORITY-005` and its selected `SYSTEM-TOPOLOGY-007` / ADR.
2. Ingest cannot create, promote, retire, re-parent or auto-provision a system.
3. PROGRAMMING remains the admitted peer owner for Programming engineering semantics; Knowledge Recovery remains CORE-administered support only.
4. WEBSITE_BUILDING remains C40 under PROGRAMMING and is not a peer system.
5. Every recovered source keeps original locator, identity/version, claims and provenance.
6. Observed digest claims never exceed bytes actually observed.
7. Conflicting claims are preserved and quarantined, never silently normalized.
8. Historical PASS never transfers across changed SHA, topology, ownership, subject or execution boundary.
9. Duplicate detection never deletes provenance.
10. Human, author, private-data, external-provider, native-platform, publication and production authority cannot be synthesized.
11. Same exact frozen subject processing must remain deterministic and idempotent except for outer receipt timestamps.
12. PROSE is retired and non-dispatchable; Book-only integration of preserved completed capability does not create Prose work.

## Manifest lineage

`PROGRAMMING-INGEST-MANIFEST-001` and `PROGRAMMING-INGEST-MANIFEST-002` remain immutable historical proving inputs. `PROGRAMMING-INGEST-MANIFEST-003` remains an immutable Topology-005 successor and historical predecessor for current recovery. Any Topology-007 qualification manifest must append to that lineage; it must not rewrite or relabel earlier manifests or receipts.

## Current next action

Continue `PROGRAMMING-EXTERNAL-BYTE-MATERIALIZATION-AND-FULL-CORPUS-INGEST-001` from the preserved Manifest 003 custody state rather than recreating it:

1. resolve the 21 not-yet-observed external records and 2 identity-unproven records to exact observations or durable explicit blockers;
2. separately move the 5 already-observed exact objects through an approved immutable A-01-readable transport without substitution, preserving their recorded lengths and SHA-256 identities;
3. preserve original source identity, hashes, conflicts, uncertainty and provenance for every record;
4. keep current Programming engineering work in the PROGRAMMING lane and use Knowledge Recovery only for missing source/custody/provenance gaps;
5. only after all inherited unresolved records have durable terminal custody dispositions, freeze the current Topology-007 inputs and generate an append-only successor manifest/catalog/trace qualification subject as needed;
6. prepare `PROGRAMMING-FULL-CORPUS-A01-QUALIFICATION-001` only when that exact current-authority subject exists and deterministic hosted prequalification passes;
7. execute A-01 only when the current registry/policy explicitly admits that exact qualification/execution context;
8. do not let the transport blocker on the 5 observed objects stop independent authorized discovery of the remaining external records or other catalog/archive recovery work.
