# GITHUB-A01-CONTROLLER-FORENSIC-RECONCILIATION-001A

Status: **FROZEN — BOOTSTRAP ADMISSION + LOSSLESS PORT MAP / NO EXECUTABLE CHANGE / NO PRODUCTION ACTIVATION**

Operation: `GITHUB-A01-CONTROLLER-FORENSIC-RECONCILIATION-001A`  
Branch: `github-a01-controller-forensic-reconciliation-001a-20260912`  
Exact base: `main@c41884bf9cc66c24fb7f245d68eefc6dbbd1139f`  
Qualified controller source: `bad4708ba91b533c333ba2659ab99bf071710ba6`  
Mastery contract source: `5905da7deadbc9d227d9976dae91804365dbe99d`

## 1. Purpose

001A opens a separately authorized controller-maintenance workstream without inventing `CG-012`. It binds current `main` as the product-preserving base, the terminal CG-011 A-01-qualified subject as the controller source baseline, and the two-commit Mastery contract subject as a preserved future integration source.

The operation is a bounded bootstrap because the repository currently lacks a production-enforced unavoidable Control Gateway mutation boundary. The bootstrap authority applies only to this isolated branch and the three 001A governance paths. It grants no production, scheduler, A-01 execution, registry, main-branch, ruleset, branch-protection, or product mutation authority.

## 2. Source authority

CG-011 remains terminal. Its frozen A-01 source subject is `bad4708ba91b533c333ba2659ab99bf071710ba6`, qualified by run `34704481930` on runner `A-01`, Windows X64, with evidence artifact `10301701961` and artifact SHA-256 `4d55819c486bbde8c0dcb7870921741312293988f40ae09827473e2be92686b0`.

That PASS does not transfer across changed SHA. It is evidence for the source baseline only.

Mastery subject `5905da7deadbc9d227d9976dae91804365dbe99d` is exactly two commits ahead of the current main base and contains exactly six Mastery contract/design/test/workflow files. It remains contract-qualified design authority only and is not production active.

## 3. Lossless port law

The machine-readable manifest is:

`governance/control-gateway/GITHUB-A01-CONTROLLER-FORENSIC-RECONCILIATION-001A-CANONICAL-PORT-MANIFEST.json`

It forbids wholesale merge or reset of the controller branch. 001B must begin from current main and selectively port or reconcile every exact controller-delta path. Every source path must receive exactly one disposition. Any unmatched path is a hard blocker.

The three authority-sensitive files that must be reconciled rather than blind-copied are:

- `.github/scripts/validate-system-topology.js`
- `.github/workflows/a01-overnight-night-shift.yml`
- `qualification/a01/registry.json`

The old GitHub overnight workflow may not retain competing scheduler authority. Current-main qualification registrations may not be lost. Current topology facts may not be replaced by stale source facts.

## 4. Proof-as-we-go rule

A-01 is used as executable proof throughout the reconciliation, but only where the candidate has host-dependent behavior to prove.

- 001A changes governance documents only. It intentionally does **not** claim or request a new A-01 runtime PASS.
- Every executable controller candidate beginning in 001B requires fresh hosted regression on its own exact SHA.
- Every A-01-dependent controller/supervisor candidate requires fresh Windows X64 A-01 qualification before that executable candidate can freeze.
- No historical PASS transfers to a changed SHA.
- Mastery runtime later requires contract regression, shadow/calibration, exact A-01 adversarial-night qualification, canary, and an explicit promotion receipt.

## 5. 001A verification

Repository verification at pre-freeze head `db114bff307f01195a135f639b3e033b8cb47c3b` established:

- exact merge base with `main` is `c41884bf9cc66c24fb7f245d68eefc6dbbd1139f`;
- branch was exactly two commits ahead and zero behind;
- the only changed paths were the bootstrap admission JSON and the canonical port manifest JSON;
- both files were re-read successfully from the exact repository head;
- no executable code, workflow, registry, A-01 control file, product file, or `main` ref was modified before freeze.

The JSON documents are structurally valid by direct exact-content verification. A hosted machine parser is intentionally deferred to 001B's candidate qualification rather than introducing an otherwise unauthorized 001A workflow solely to test documentation.

## 6. Explicit non-authority

001A does not:

- update `main`;
- move or rewrite the CG-011 frozen ref;
- create CG-012;
- production-activate the controller;
- production-activate Mastery;
- grant a direct self-hosted workflow bypass;
- alter GitHub rulesets or branch protection;
- import the unrelated Planning/Orchestration workstream;
- authorize a wholesale merge of `bad4708...`.

## 7. Exact successor

`GITHUB-A01-CONTROLLER-FORENSIC-RECONCILIATION-001B — MAIN-DERIVED CONTROLLER CANONICALIZATION CANDIDATE + LOSSLESS SOURCE PORT / CONFLICT RECONCILIATION + HOSTED-THEN-A01 EXACT-SHA PROVING`

001B may start only from this 001A branch head and must preserve current-main product state while constructing the first executable canonical controller candidate.
