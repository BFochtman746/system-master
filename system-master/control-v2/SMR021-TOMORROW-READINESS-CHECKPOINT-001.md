# SMR021 Tomorrow Readiness Checkpoint 001

Date: 2026-09-09
Owner lane: `SYSTEM_MASTER/CORE`
Status: **RESUMABLE CHECKPOINT / CURRENT IMPLEMENTATION BLOCKED ONLY BY EXACT SMR020 RUNNABLE SOURCE CUSTODY**

## 1. Authority standing

Live CORE control ref during this bounded work:

`system-master/control-v2`

The startup authority delta through `18d0b8eeb9d1a1cd875583cce1cea478f110ef68` was reconciled. The added PLATFORM-003 fresh Library replay is historical replay evidence and did not change the active SMR021 dependency-current objective.

This checkpoint advances only preparation/research evidence. It does not claim implementation closure.

## 2. Completed phases that must not be restarted

The following are now durable completed phases:

1. historical R025 v2.0.24 source-custody recovery and constituent portable replay;
2. adversarial R025 reconciliation and historical database-parity defect discovery;
3. corrected historical R025 database-parity candidate qualification;
4. dependency-current `ca2c9348...` overlay adversarial diagnosis that reproduced the cross-work/unbound-action admission bypass;
5. broad exact-SMR020 source-custody recovery audit across current GitHub and Library surfaces, including the September 9 ZIP census;
6. implementation-ready admission-binding + R025 database-parity repair packet;
7. current external authorization-context binding research delta;
8. current offline/server-confirmation + portable accessibility research delta.

Do not rerun these merely because a chat or delegation binding changes.

## 3. Durable evidence produced in this continuation

### Implementation-ready contract

`SMR021-ADMISSION-BINDING-TOMORROW-READINESS-001.md`

Seals:

- exact predecessor verification-before-mutation gate;
- historical/corrected/failed/future subject separation;
- current-owner descriptor action-grant pre-mutation stop gate;
- minimum admission work/descriptor/action binding contract;
- R025 database-parity transplant requirements;
- focused adversarial micro-gate;
- full portable qualification order;
- hosted exact-SHA and A-01 boundaries;
- empirical evidence fences.

### Authorization research delta

`SMR021-AUTHORIZATION-CONTEXT-BINDING-RESEARCH-DELTA-001.md`

External evidence supports exact resource/work + action binding, request-time authorization validation, authorization-context integrity/anti-swapping, deny-by-default and confused-deputy defenses without adopting a generic OAuth/zero-trust subsystem.

The focused authorization gate is strengthened to a minimum **20 named cases**, including:

- same-work/different-action rejection;
- stale route after descriptor change rejection;
- cross-route field-swap rejection;
- missing owner-defined action-grant fail-closed.

### Offline/accessibility research delta

`SMR021-OFFLINE-ACCESSIBILITY-RESEARCH-DELTA-001.md`

Findings:

- preserve historical R025 behavior that treats `navigator.onLine` only as a hint and reserves `Connected — server confirmed` for an actual validated `/api/session` response;
- verify authority-bearing endpoints cannot be satisfied by stale cache while presented as remote confirmation;
- preserve API freshness identities when current contracts expose them;
- add explicit whole-message status semantics where intended;
- retain System Master's stronger historical 44px interactive-target policy;
- add bounded focus-obscuration and Files-surface focus-contract tests;
- do not convert these portable/headless checks into physical-device or screen-reader PASS.

## 4. Exact current blocker

Required runnable predecessor source/test digest:

`89066e5662554b1810691473e0ed9bf49d8cc2fd4964444043b878461ed75477`

Current durable custody disposition:

**BLOCKED — exact runnable predecessor bytes are not present in the already-audited current GitHub or Library search surfaces.**

A valid release from this blocker requires either:

1. original runnable SMR020 source/archive whose independently recomputed source/test digest equals exact `89066e56...`; or
2. sufficient exact predecessor + executable reconstruction artifacts to deterministically reproduce that same digest.

A different digest is a new candidate lineage and cannot inherit SMR020 PASS.

## 5. First executable action when custody appears

Execute exactly this sequence:

1. acquire candidate SMR020 runnable bytes from the genuinely new custody surface;
2. independently recompute source/test digest **before mutation**;
3. if digest != `89066e56...`, classify `DIFFERENT_BYTES_NEW_LINEAGE` and do not call it recovered SMR020;
4. if exact, freeze/snapshot the recovered predecessor unchanged;
5. inspect the exact current PLATFORM-006 descriptor/admission contract for an owner-defined immutable action grant;
6. if no owner-defined action grant exists, stop with `CROSS_OWNER_PLATFORM006_ACTION_GRANT_CONTRACT_REQUIRED` rather than minting UX authority;
7. otherwise apply only the minimum work/descriptor/action binding repair and dependency-current equivalent of the R025 database-parity transplant;
8. execute the minimum 20-case authorization-focused adversarial micro-gate first;
9. on focused PASS, run strict Java + full applicable executable regression + Java/schema/SQL parity + R025 recurrence + persistence/concurrency + offline/accessibility bounded gates + coherence/congruence + exact-subject + release-manifest qualification;
10. assign the changed tree a new exact source/test subject identity;
11. place that exact subject into ordinary GitHub-native immutable custody and obtain hosted exact-SHA evidence before any A-01 eligibility decision.

## 6. Stop/fail rules

- Do not use historical v2.0.23 or v2.0.24 as the dependency-current base.
- Do not resurrect `ca2c9348...` as a closable subject.
- Do not transfer PASS between exact subjects.
- Do not repeat broad custody search unless a genuinely new custody surface or artifact becomes available.
- Do not invent a descriptor action grant from generic metadata such as purpose text, semantic tags or protocol names.
- Do not let a cached response become remote/server confirmation.
- Do not weaken database, authorization, accessibility or identity tests to obtain PASS.
- Do not claim physical-device, assistive-technology, usability, live-target-PostgreSQL, A-01 or production authority without exact evidence.

## 7. Remaining useful unattended work

The high-value reusable preparation is now complete. Further broad authorization/offline/accessibility research would be repetition before current-source inspection.

Second Shift should therefore:

- react to genuinely new custody evidence if it appears;
- preserve this checkpoint and all predecessor evidence;
- avoid re-running completed research merely to create activity;
- execute the sealed repair immediately if exact custody is released;
- otherwise hand off this exact resume point unchanged.

## 8. Exact next step

`CORE-SMR021-EXACT-SMR020-CUSTODY-RELEASE-AND-EXECUTE-001`

**Trigger:** a genuinely new candidate source/archive/reconstruction input for SMR020 becomes available.

**Action:** verify exact `89066e56...` before mutation, then execute `SMR021-ADMISSION-BINDING-TOMORROW-READINESS-001.md` plus both research deltas from the first executable gate.

**Until trigger:** remain `BLOCKED — SOURCE_CUSTODY`, preserve completed work, and do not restart discovery from zero.
