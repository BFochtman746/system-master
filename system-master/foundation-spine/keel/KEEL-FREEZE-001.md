# CORE Intent / Keel Freeze 001

## Standing

`CORE-KEEL-BUILD-001` is frozen at **CURRENT — HOSTED QUALIFIED** only when the exact commit containing this freeze record passes `.github/workflows/foundation-keel-qualification.yml` without qualification narrowing.

The freeze is bounded to hosted-portable reference authority mechanics. It does **not** claim A-01 execution, native sudden-power-loss durability, distributed/network-filesystem writer safety, production storage/admission, real principal/approval/contract-provider validity, human response truth, specialist-domain semantic validity or production activation.

## Frozen authority boundary

Keel owns only governed intent:

- stable goal identity;
- immutable governed goal revisions;
- problem/outcome, requirements and hard/soft constraints;
- success declarations and required evidence classes;
- delegation, resource and action ceilings;
- human-control requirements;
- explicit parent/child intent lineage and non-expansion validation;
- governed/superseded/retired standing;
- exact downstream `GovernedGoalRefV1` and `KeelValidationReceiptV1`.

Keel does not own or mint concrete Identity grants, resource reservations/grants/accounting, Work/Project lifecycle, executable plans, capability routes, placement assignments, runtime jobs/attempts/leases/fences, external-effect permission, human approval truth, completion evidence or Book/Learning/Documents/Programming specialist semantics.

## Durable reference implementation

The hosted-portable implementation is `KeelAuthorityRuntime` under `system-master/foundation-spine/keel/src/main/java`.

Canonical mutations are append-durable, hash chained and projection-reconstructable. They use semantic command identity (`commandId + requestHash`), optimistic registry revision checks, exact parent revision/digest binding, and fail-closed journal replay. Accepted publication binds an exact Contracts gate receipt. Human approval requirements bind only an opaque externally validated evidence reference; the runtime never fabricates a participant response.

A child revision may preserve or narrow authority but may not widen inherited hard constraints, delegation/resource/action ceilings, forbidden-action boundaries or human-control requirements. Unknown critical comparator state fails closed.

## Lossless traceability

`KEEL-BUILD-TRACEABILITY.json` is the current implementation census for this component. It accounts **32/32 bounded Keel invariants with zero unaccounted rows** using the required chain:

**requirement/invariant -> implementation -> durable state -> interface/contract -> tests -> evidence -> environment -> blocker**.

Historical F-WP material is reused only as donor provenance/mechanics. Historical PASS is not transferred to the changed Keel subject.

## Qualification denominator

The frozen isolated denominator is exactly **56 adversarial cases**, covering:

- goal identity/revision/publication and replay;
- hard/soft constraint preservation and unknown-comparator failure;
- delegation/resource/action/HITL non-expansion;
- approval/evidence/specialist ownership fences;
- journal replay, corruption, truncation and concurrent conflicts;
- exact Contracts-gate binding and bounded Work-Chain reference output.

The cumulative qualification runner also requires current System Root + O-WP-001/O-WP-002 + Identity Delegation + Contracts & Versioning qualification through the current Contracts runner, plus F-WP-002 and F-WP-005 donor regression. Evidence is valid only for the exact tested candidate commit/tree.

## Evidence fences

The following remain separate evidence classes and blockers rather than inferred PASS:

- A-01 target execution;
- native sudden-power-loss durability;
- distributed or network-filesystem writer exclusion/safety;
- production persistence/admission;
- real external identity, contract-gate and approval authorities;
- human approval/participant-response truth;
- Book, Learning, Documents or Programming semantic correctness.

## Dependency-valid successor

The next Foundation/Spine operation is:

**`CORE-WORK-CHAIN-RECOVERY-INVENTORY-001 — CANONICAL CAUSAL LINEAGE LOSSLESS CENSUS + OWNER/STATE/CONTRACT/EVIDENCE BINDING`**

Recover and map the canonical lineage from exact governed goal revision through Work/Project, plan/step, policy decision set, provisional admission, capability route, resource grant, assignment, durable job, attempt/fence, context, invocation, effect receipt, state/artifact, evidence, completion and recovery. Reuse existing F-WP/recovered substrate; do not create a second truth owner and do not absorb specialist semantics. Advance to design lock only when the path-level census is lossless and owner collisions are adjudicated.
