# LEARNING SYSTEM — CONTROL RECORD v1

Status: **ACTIVE / PRIMARY LEARNING CONTROL AUTHORITY**  
System ID: `LEARNING`  
Canonical branch: `learning/control-v1`  
Topology authority: `governance/SYSTEM-TOPOLOGY-001.json` on canonical `main`

## Purpose

This branch is the durable control surface for the **Learning System**. It exists so implementation, pilot, request, qualification and repair branch names cannot silently become the system's current control authority.

## Topology

The Learning System is one of exactly four canonical systems: MASTER, LEARNING, BOOK and PROSE. It is not a Master subsystem and it is not created or superseded by an A-01 workstream, pilot branch or scheduled chat.

## Current qualified software boundary

This control branch was created from exact qualified/promoted subject:

`f49ac9eaf6e0f11894bf47f5d773090b4ef60ebf`

That subject passed authoritative A-01 final-human-execution software qualification before this doc-only control branch was created. This control commit does **not** transfer that qualification to changed bytes; exact-SHA evidence remains bound to the tested subject.

The machine-side final-human-execution boundary is complete. Real participant execution remains **BLOCKED — HUMAN** pending genuine explicit consent and real learner evidence.

## Current system-development rule

The human pilot blocker does not freeze Learning System development. Continue dependency-valid non-human work such as learning architecture, learner/mastery modeling, adaptive sequencing, assessment validity, retention/transfer design, misconception handling, privacy-preserving evidence, accessibility, deterministic software qualification and research-backed build-ahead. Do not fabricate participant consent, responses, retention outcomes, transfer outcomes or human attestation.

## Branch interpretation

Historical `learning/impl-*`, `learning/qual-*`, `learning/request-*`, `learning/pilot-*`, `learning/proposed-*`, import and repair branches are implementation/evidence history unless this control record explicitly selects one as the current successor.

A newer timestamp or a more specific branch name does not outrank this control record.

## A-01 routing

A-01 `workstream_id=LEARNING` is an execution/qualification lane owned by the Learning System. It is not a separate system. No new Learning A-01 ticket is justified solely because the runner has capacity.

## Next-step authority

A future Learning successor must be recorded here or in an explicitly superseding Learning control record. Generic `Continue` means continue the highest-value dependency-valid Learning System objective, not replay the latest historical pilot or qualification branch.

## Qualification boundary

This record creates control authority only. It grants no participant, human, production, publication, native-platform or broadened exact-SHA qualification authority.
