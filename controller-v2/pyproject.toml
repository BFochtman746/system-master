[project]
name = "system-master-controller-v2"
version = "0.0.1"
description = "Clean-sheet transactional foundation for the System Master GitHub Controller / Second Shift"
requires-python = ">=3.14"
dependencies = []

[tool.unittest]
start-directory = "tests"
