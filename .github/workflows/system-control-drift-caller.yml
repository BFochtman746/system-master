name: Book System Control Drift

on:
  push:
    paths:
      - 'qualification/book-system/**'
      - '.github/workflows/system-control-drift-caller.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: BFochtman746/system-master/.github/workflows/system-control-drift-reusable.yml@main
    with:
      control_branch: book-system/control-v1
      subject_sha: ${{ github.sha }}
