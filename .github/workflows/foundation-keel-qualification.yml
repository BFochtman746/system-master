name: Foundation Intent Keel Qualification

on:
  pull_request:
    branches:
      - system-master/control-v2
    paths:
      - 'system-master/foundation-spine/keel/**'
      - '.github/scripts/foundation-keel-qualify.js'
      - '.github/workflows/foundation-keel-qualification.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify-keel:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout exact candidate with full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Qualify Keel and cumulative Foundation baseline
        run: node .github/scripts/foundation-keel-qualify.js

      - name: Preserve qualification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: foundation-keel-evidence-${{ github.run_id }}
          path: ${{ runner.temp }}/foundation-keel-*/
          if-no-files-found: error
          retention-days: 30
