name: BOOK Reconstruction B00 F2 Qualification

on:
  push:
    branches:
      - book-system/reconstruction-b00-parent-v2
    paths:
      - 'system-master/book-system/canonical-parent-v2-core.js'
      - 'system-master/book-system/canonical-parent-v2-sqlite-store.js'
      - '.github/scripts/book-system-canonical-parent-v2-core-001-qualify.js'
      - '.github/scripts/book-system-canonical-parent-v2-store-001-qualify.js'
      - '.github/workflows/book-reconstruction-b00-f2-qualify.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify:
    name: Node ${{ matrix.node }} — Q001-Q050
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: ['22', '24']
    steps:
      - name: Checkout exact subject
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - name: Record exact subject
        run: |
          echo "subject_sha=${GITHUB_SHA}" | tee b00-f2-subject-node-${{ matrix.node }}.txt
          git rev-parse HEAD | tee -a b00-f2-subject-node-${{ matrix.node }}.txt
          node --version | tee -a b00-f2-subject-node-${{ matrix.node }}.txt
      - name: Run frozen F1 Q001-Q036 denominator
        run: |
          node .github/scripts/book-system-canonical-parent-v2-core-001-qualify.js | tee b00-f2-core-node-${{ matrix.node }}.log
      - name: Run F2 Q037-Q050 durable-store denominator
        run: |
          node .github/scripts/book-system-canonical-parent-v2-store-001-qualify.js | tee b00-f2-store-node-${{ matrix.node }}.log
      - name: Hash evidence
        if: always()
        run: |
          sha256sum b00-f2-subject-node-${{ matrix.node }}.txt b00-f2-core-node-${{ matrix.node }}.log b00-f2-store-node-${{ matrix.node }}.log > b00-f2-node-${{ matrix.node }}.sha256
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: book-reconstruction-b00-f2-node-${{ matrix.node }}-${{ github.sha }}
          path: |
            b00-f2-subject-node-${{ matrix.node }}.txt
            b00-f2-core-node-${{ matrix.node }}.log
            b00-f2-store-node-${{ matrix.node }}.log
            b00-f2-node-${{ matrix.node }}.sha256
          if-no-files-found: error
