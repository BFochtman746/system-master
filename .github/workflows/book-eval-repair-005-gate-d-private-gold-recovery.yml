name: BOOK-EVAL-LEMONADE-001 REPAIR-005 Gate D Private Gold Recovery

on:
  push:
    branches:
      - book-eval-lemonade-001-blind-resume-003-adapter-repair
    paths:
      - "qualification/book-eval-lemonade-001/repair005/RUN-GATE-D-PRIVATE-GOLD-RECOVERY-CONTROL-PLANE"
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  qualify:
    name: Recover exact Gate B or scoring-private authority through A-01 control plane
    uses: BFochtman746/system-master/.github/workflows/a01-control-plane-gateway.yml@main
    with:
      qualification_id: BOOK-EVAL-REPAIR-005-GATE-D-PRIVATE-GOLD-RECOVERY
      workstream_id: BOOK-EVAL-LEMONADE-001
      subject_sha: ${{ github.sha }}
      origin_ref: ${{ github.ref }}
      resume_on_pass: "Adjudicate exact-hash private authority recovery; if exact binding is proven, resume Gate D at Teacher v3 freeze verification without touching visible-regression or hidden-holdout."
      resume_on_failure: "Adjudicate receipt and qualifier evidence; repair only PRIVATE-GOLD-RECOVERY for subject failure, or the A-01/control-plane boundary for infrastructure/control-plane failure; do not run Teacher, student, or 120B."
      notification_target: book-system-chat
