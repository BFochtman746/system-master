name: Learning 001E Batch 05 Prequalification

on:
  push:
    branches:
      - learning/instructional-scoring-001e-batch-05-20260910
    paths:
      - "learning/lab/learning_lab/cssgb_full_standard_i_b_2.py"
      - "qualification/learning/LEARNING-FULL-STANDARD-CURRICULUM-COMPILER-001E-INSTRUCTIONAL-CONTENT-AND-SCORING-BATCH-05-CANDIDATE-068A.json"
      - ".github/scripts/learning-full-standard-001e-batch-05-qualify.py"
      - ".github/workflows/learning-001e-batch-05-prequal.yml"
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  qualify:
    name: Ground, score, and execute I.B.2 runtime slice
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PYTHONPATH: learning/lab
    steps:
      - name: Checkout exact Batch 05 subject with history
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Record exact subject and immutable 001D input
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${GITHUB_SHA}"
          git cat-file -e "02dc615fcb5d44b997ed1322848eccc6e37bb172^{commit}"
          printf 'SUBJECT_SHA=%s\n' "$GITHUB_SHA"

      - name: Compile-check Batch 05 runtime and qualifier
        shell: bash
        run: |
          set -euo pipefail
          python -m py_compile \
            learning/lab/learning_lab/adaptive.py \
            learning/lab/learning_lab/cssgb_full_standard_i_b_2.py \
            learning/lab/tests/test_adaptive_practice_not_mastery.py \
            .github/scripts/learning-full-standard-001e-batch-05-qualify.py

      - name: Preserve adaptive practice-not-mastery regression
        shell: bash
        run: |
          set -euo pipefail
          python -m unittest learning.lab.tests.test_adaptive_practice_not_mastery

      - name: Execute 001E Batch 05 qualification
        shell: bash
        run: |
          set -euo pipefail
          export EVIDENCE_DIR="$RUNNER_TEMP/learning-001e-batch-05-evidence"
          python .github/scripts/learning-full-standard-001e-batch-05-qualify.py

      - name: Upload Batch 05 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: LEARNING-001E-BATCH-05-${{ github.sha }}-hosted-evidence
          path: ${{ runner.temp }}/learning-001e-batch-05-evidence
          if-no-files-found: warn
          retention-days: 30

# Portable I.B.2 oracle/runtime qualification only. The immutable full 001D blueprint remains authoritative for the admitted I.B.1 -> I.B.2 hard prerequisite. No real-learner, psychometric, SME, certification, A-01, native-platform, or production authority is inferred.
