name: A-01 Repair Resume Once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/a01-repair-resume-once.yml'

permissions:
  contents: read
  actions: read

jobs:
  rerun:
    uses: ./.github/workflows/a01-repair-rerun.yml
    with:
      transaction_id: A01-REPAIR-a01-run-34431764843-A01-CONTROL-PLANE-SELFTEST-A1
      execution_context: normal
      qualifier_timeout_minutes: 28
      job_timeout_minutes: 30
