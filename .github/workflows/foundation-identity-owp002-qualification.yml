name: Foundation Identity O-WP-002 Qualification

on:
  pull_request:
    branches:
      - system-master/control-v2
    paths:
      - 'system-master/foundation-spine/identity/**'
      - '.github/scripts/foundation-identity-owp002-qualify.js'
      - '.github/workflows/foundation-identity-owp002-qualification.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify-identity-owp002:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact candidate with full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Qualify O-WP-002 and run cumulative Foundation regressions
        run: node .github/scripts/foundation-identity-owp002-qualify.js

      - name: Preserve qualification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: foundation-identity-owp002-evidence-${{ github.run_id }}
          path: ${{ runner.temp }}/foundation-identity-owp002-*/
          if-no-files-found: error
          retention-days: 30
